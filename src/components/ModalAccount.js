import Modal from "./Modal";

const ModalAccount = ({title, root}) => {
    return (
        <Modal title={title} root={root}>
            Modal de cuenta
        </Modal>
    );
};

export default ModalAccount;